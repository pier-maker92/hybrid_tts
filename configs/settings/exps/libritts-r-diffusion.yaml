# @package _global_
training:
  dataset_name: libritts-r
  output_dir: checkpoints/diffusion-only/libritts-r-voice
  per_device_train_batch_size: 24
  gradient_accumulation_steps: 1
  logging_steps: 10
  save_total_limit: 2
  # report
  report_to: none
  wandb_project: hybrid_tts
  wandb_run_name: libritts-r-diffusion
  run_id: ""
  bf16: true
  # eval
  eval_strategy: "no"
  eval_steps: null
  save_steps: 1000
  eval_num_samples: 4
  num_train_epochs: 10
  # lr
  learning_rate: 3e-4
  min_learning_rate: 1e-5
  lr_scheduler_type: "cosine"   
  warmup_ratio: 0.05         
  # cfg
  uncond_prob: 0.1
  no_augment_ratio: 0.25
  discrete: true
  continuous: true
  online_encode: true
  group_by_length: false
  pretrained_checkpoint: "checkpoints/diffusion-only/libritts-r"

diffusion_head: 
  net_dim: 512
  net_depth: 8
  net_heads: 8
  uncond_prob: 0.1
  use_mlp_sampler: false
  is_causal: false
  use_conv_layer: true
  use_window_attention: false
  voice_condition: true

vae_checkpoint: "$SCRATCH/MelCausalVAE/checkpoints/set15-64"
vocoder_checkpoint: "vocos"
debug: false
